apiVersion: batch/v1
kind: Job
metadata:
  name: cube-test-job
  namespace: domino
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: test
        image: alpine
        command:
          - /bin/sh
          - -c
          - |
            echo "Hello world from cube-control";
            date;
            sleep 5;
            echo "Job completed."
