# Certificate directory for NGINX

This directory should contain the certificate files needed for NGINX.

The following three files should be available before starting the compose stack:

- cert.pem
- key.pem
- password.txt

